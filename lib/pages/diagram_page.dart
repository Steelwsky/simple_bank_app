import 'package:flutter/material.dart';

class DiagramPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('diagram here'),
    );
  }
}
